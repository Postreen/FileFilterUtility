# File Filter Utility

CLI-утилита на Java для фильтрации строк из входных файлов по типам данных:
- целые числа;
- числа с плавающей точкой;
- строки.

Результаты сохраняются в отдельные выходные файлы, а утилита может дополнительно выводить статистику (summary/full).
Логи сохраняются в папку `logs`.
---

## 📌 Описание проекта

`FileFilterUtility` читает один или несколько входных файлов построчно, определяет тип каждой строки и распределяет значения по файлам:
- `integers.txt`
- `floats.txt`
- `strings.txt`

Поддерживаются:
- выбор директории вывода -o <outputDir>. Если не указать, то файлы сохранятся в корне проекта;
- префикс для имен выходных файлов -p <myPrefix>;
- режим дозаписи -a>;
- вывод краткой или полной статистики -s / -f.

---

## ⚙ Требования

### Runtime / Build
- **Java:** 17
- **Gradle:** 8.x (в репозитории используется Gradle Wrapper, рекомендуется запуск через `./gradlew`)

### Сторонние библиотеки
- `commons-cli:commons-cli:1.9.0`
- `org.springframework:spring-context:7.0.3`
- `ch.qos.logback:logback-classic:1.5.27`
- `org.projectlombok:lombok:1.18.38` (compile-time)
- `org.junit:junit-bom:5.10.0` + `org.junit.jupiter:junit-jupiter` (tests)

---

## 🚀 Запуск

---

## 🧪 Примеры запуска с аргументами

### Краткая статистика
```bash
  ./gradlew run --args='-s ./src/main/resources/in1.txt ./src/main/resources/in2.txt'
```

### Полная статистика + префикс файлов
```bash
  ./gradlew run --args='-f -p result_ ./src/main/resources/in1.txt ./src/main/resources/in2.txt '
```

### Режим дозаписи
```bash
  java -jar build/libs/FileFilterUtility-1.0.jar -a ./src/main/resources/in1.txt ./src/main/resources/in2.txt '
```